{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start OpenOCD (STM32 Blue Pill Rust)",
            "type": "process",
            "command": "c:\\openocd\\bin\\openocd",
            // "command": "openocd -f interface/stlink-v2.cfg -f target/stm32f1x.cfg",
            "problemMatcher": []
        },
        {
            "label": "Build (STM32 Blue Pill Rust)",
            "type": "shell",
            "command": "export PATH=\"$HOME/.cargo/bin:$PATH\"; cargo build -v --release",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Remove Build (STM32 Blue Pill Rust)",
            "type": "shell",
            "command": "export PATH=\"$HOME/.cargo/bin:$PATH\"; cargo clean -v",
            "problemMatcher": []
        },
        {
            "label": "Check Build (STM32 Blue Pill Rust)",
            "type": "shell",
            "command": "export PATH=\"$HOME/.cargo/bin:$PATH\"; cargo check -v --release",
            "problemMatcher": []
        }
    ]
}